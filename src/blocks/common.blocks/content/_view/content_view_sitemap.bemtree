block('content').mod('view', 'sitemap').content()(function () {
    var data = this.data;

    return [
        {
            tag: 'h2',
            content: data.node.title[data.req.lang]
        },
        {
            block: 'sitemap',
            node: this.data.menu
        }
    ];
});
